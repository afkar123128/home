import 'package:flutter/material.dart';

class alarmcolock extends StatelessWidget {
  const alarmcolock({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("alarmcolock")),
    );
  }
}